# AI Agents

An AI agent combines a language model with tools, instructions, state,
and a control loop. The model can decide what action is needed, call an
available tool, observe the result, and continue until the task is done.

A normal language-model call produces one response. An agent can perform
actions and react to their results.
