# Retrieval-Augmented Generation

Retrieval-augmented generation gives a language model external knowledge
before it creates an answer. The user question is converted into an
embedding and compared with document embeddings. Relevant passages are
then added to the model prompt.

Retrieval helps reduce unsupported answers because the model receives
information from the selected knowledge source.
